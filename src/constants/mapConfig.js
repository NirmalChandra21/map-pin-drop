export const MAP_CONFIG = {
    // Default center coordinates (London)
    defaultCenter: [51.505, -0.09],
    defaultZoom: 13,

    // Map tile layer URL - Replace with your preferred provider
    tileLayerUrl: "https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",

    // If using Mapbox or Google Maps, add your API key here
    apiKey: "YOUR_API_KEY",
};
